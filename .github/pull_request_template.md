## 概述

（本次变更做了什么、为什么）

## 关联任务

（multica 任务键，如 WORD-NN；无则写「无」）

## 变更点

-

## 自查清单

- [ ] `godot --headless --path . res://tests/test_runner.tscn` 全量测试通过
- [ ] 已在根目录 `changelog.md` 的「**未发布**」区追加条目（不影响行为的改动可给本 PR 打 `skip-changelog` 标签豁免）
